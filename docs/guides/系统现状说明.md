# service-hub 系统现状说明（2026-09-19）

> 目标实例：`${HUB_HOST}`（阿里云 Ubuntu 24.04 / x86_64 / 2 核 3.4 G / 40 G 盘）
> 代码分支：`feature/v2-auth-rbac-audit`（worktree `.worktrees/service-hub-web-console-impl`）
> 配套文档：`服务器部署实施清单.md`（操作手册）、`完全部署剩余任务清单.md`（任务追踪）、
> `架构审查-缺陷与优化清单.md`（缺陷台账 D-12~D-30）

---

## 一、系统现状

### 1.1 运行拓扑（三容器，实测 Up / healthy）

| 容器 | 角色 | 端口映射 | 状态（09-19 07:05 实测） |
|---|---|---|---|
| `service-hub-service-hub-1` | 后端单容器多进程（tini+supervisord） | `127.0.0.1:18000 → 8000` | Up / healthy |
| `service-hub-service-hub-web-1` | Nginx + SPA 控制台 | `127.0.0.1:18080 → 8080` | Up / healthy（**已含 D-25 修复**） |
| `service-hub-edge-1` | nginx:1.27-alpine TLS 终止 | `0.0.0.0:8443 → 8443`（**唯一对外端口**） | Up 20 小时 |

后端容器内受管 6 进程（`supervisord.conf` 权威名单，宿主机套件实测全 RUNNING）：
`hub-api` / `conda-runner` / `docker-runner` / `cleaner` / `scheduler` / `service-manager`。

**今日更新**：web 容器已滚动到含 D-25 修复的镜像（bundle 实测含 `/pipelines/runs`、
旧路径 `/pipeline-runs` 已消失）。注意 web 容器重建会连带滚动后端容器
（`depends_on: service_healthy`），当前两者均为新起且 healthy。

### 1.2 对外访问（已验证）

- 访问地址：**`https://${HUB_HOST}:8443`**（自签证书，浏览器需手工信任一次）
- 证书：`CN=${HUB_HOST}`，有效期 **2026-09-17 → 2028-12-20**（约 26 个月，无需近期更换）
- OS 防火墙：`ufw inactive`、`iptables INPUT policy ACCEPT`（系统层全开），
  8443 的放行由**云安全组**控制 —— 今日实测外网访问正常，安全组规则有效。
- **同机还有另一个容器 `cs-nginx` 占用 80/443**（已运行 4 周，非本项目）——
  这是本栈使用 8443 的原因；**不要动它，也不要尝试让本栈抢 80/443**。

### 1.3 部署的代码世代（2026-09-19 23:10 更新：**已与本地分支对齐**）

运行中的部署含 V4.0 插件内核（`hub_server/kernel/` + 10 个 feature plugin：
audit / auth / backup / catalog / files / jobs / metrics / quotas / schedules / webhooks）。

**本节在 21:00 复核时记录的「落后 2 个提交」已经消除**，当前 DB 迁移版本为
`0011_schedule_cron`：

| 功能 | commit | 服务器现状 |
|---|---|---|
| 大文件分块上传 `/files/chunk/*` | `4984d86` | ✅ 已部署（三路由实测 201/200/201） |
| cron 定时 `cron_expr` / `missed_run_policy` | `46ddfc1` | ✅ 已部署（仅带 `cron_expr` 创建返回 201） |
| 迁移 `0011_schedule_cron` | 同上 | ✅ 已应用（`schedules` 表实测含两列） |
| 控制台 cron 表单 | `57b9231` | ✅ 已部署（bundle 实测含 `cron_expr`/`missed_run_policy`/`catch_up`） |

本轮为完成对齐所做的提交：`b64956a`（前端类型与分块上传）、`873ff37`（D-31/D-32 修复）、
`13a4502` + `9adb1e0`（构建镜像源参数）、`121d214`（集成测试镜像源转发）、`57b9231`（cron 表单）。

运行中的镜像：后端 `python-service-hub:1.0.0-linux-amd64` = `2f43680cfe79`（275 MB）、
Web `python-service-hub-web:1.0.0-linux-amd64` = `dd483996ef5b`（49.7 MB）。

### 1.4 已通过的验收（全部有实测输出）

| 项目 | 结果 |
|---|---|
| 外网 8443 → 登录 → 全链路 API（探针 8 项） | ✅ 全过（SPA/登录/插件目录/管道/运行记录/任务/旧路径正确 404） |
| 新功能接口（分块上传三段 + cron 创建） | ✅ 全过（`chunk/init` 201 → `PUT` 200 → `complete` 201；cron 201） |
| 产物级核查（运行中 web 容器 bundle） | ✅ `chunk/init`、`cron_expr`、`missed_run_policy`、`catch_up`、`pipelines/runs` 均在 |
| 插件镜像构建 → `.pypkg` → install → READY → ENABLED | ✅ 全通 |
| 容器内集成测试（auth / quota / automation） | ✅ `3 passed`（automation 连跑三次验证幂等） |
| 宿主机集成测试（single container / web console） | ✅ `2 passed`（6 进程 / 重启持久化 / 权限边界 / 挂载边界） |
| 本机质量门 ruff / mypy / vitest / tsc / eslint | ✅ 全绿（前端 30 文件 **193** 测试；ruff 通过；mypy 92 文件通过） |
| 本机 `pytest -m "not integration"` | ✅ **930 passed / 0 failed / 3 skipped**（D-31、D-32 修复后失败归零） |
| Web 控制台「运行记录」数据（D-25 修复点） | ✅ 实测返回运行记录，无 404 |

关键产物数据（供回溯）：

- 插件镜像 digest：`sha256:f628ae9a1360…421d5049a56c4`
- `.pypkg`：`/opt/service-hub/plugin-out/nc_to_shp-1.0.0-linux-amd64-docker.pypkg`
  （227 MiB，`package_sha256=f20455ed…`）
- `runtime_fingerprint` 与构建镜像 digest 完全一致（同机构建+安装是硬约束）。

### 1.5 部署怎么管（命令速查）

```bash
# 在服务器上（SSH 后）
cd /opt/service-hub/src
export HUB_HOST_DATA_DIR=/opt/service-hub/data
docker compose -p service-hub -f compose.yaml -f /opt/service-hub/compose.override.yaml ps

# 重建/滚动某个服务（例如 web）
docker compose -p service-hub -f compose.yaml -f /opt/service-hub/compose.override.yaml up -d service-hub-web

# 看后端容器内 6 进程状态（supervisorctl 的 socket 路径在容器内非默认位置，用 python 探针）
docker exec -u root service-hub-service-hub-1 python -c \
  "import supervisor.xmlrpc as x, xmlrpc.client as c; ..."   # 或直接看 docker logs
```

> **注意**：裸 `docker compose --project-directory /opt/service-hub/src` **不能工作**
> （缺 `-p service-hub`、缺 override 文件、缺 `HUB_HOST_DATA_DIR`）。集成测试也按
> 同样口径通过 `HUB_COMPOSE_COMMAND` 环境变量传入。

---

## 二、还需要完善的问题与修改方案

### 2.1 原 P0 两件 —— **T16 已完成，T3-B 受本机环境限制**

**T16：把最新两个功能部署到服务器（分块上传 + cron 定时）** → ✅ **已完成**

执行顺序与结果：

1. 三个未提交前端文件先跑质量门（typecheck / eslint / vitest 192 passed）后提交 `b64956a`；
2. **升级前双备份**：热备 `data-backup-pre-t16.tar.gz`（943 MB）+ 停容器后的冷备
   `data-cold-20260919`（1.1 GB），均保留在 `/opt/service-hub`；
3. `git archive HEAD` 打包（0 个 CRLF）上传解包到 `/opt/service-hub/src`；
4. 重建两个镜像 → `compose up -d` → 后端自动迁移到 `0011_schedule_cron`；
5. 验证：DB 版本 = `0011`；三个 chunk 路由可用；仅带 `cron_expr` 创建返回 201；
   6 个受管进程 RUNNING；18000/18080/8443 均 200。

**T3-B：真实浏览器端到端走查** → ❌ **本机无法执行，已用产物级验证替代**

- 阻塞原因：agent-browser 自带 Chrome 在本机沙箱内启动即退出（exit code 3），
  加 `--no-sandbox` 仍失败，属环境限制而非产品问题。
- 替代验证（已完成）：直接在运行的 web 容器内 grep 构建产物，确认
  `chunk/init`、`cron_expr`、`missed_run_policy`、`catch_up`、`pipelines/runs` 全部存在；
  再叠加 API 级 8/8 全通过。
- 建议：由运维在本机浏览器访问 `https://${HUB_HOST}:8443` 人工确认一次渲染与交互。

### 2.2 P1 —— 交付完整性

**T5：服务器端质量门** → ✅ **已完成**

- `pytest.importorskip("h5py")` 已在 `tests/plugins/test_nc_to_shp.py` 就位（此前某轮已加），
  且本机 `.venv` 已具备 numpy / scipy / h5py，该模块可真正执行；
- 本机 `pytest -m "not integration"` 完整跑完：**930 passed / 0 failed / 3 skipped**（约 7 分钟）；
- 顺带发现并修复了两个**真实**缺陷 D-31、D-32（原以为是本机环境性失败，取证后确认不是），
  详见 `架构审查-缺陷与优化清单.md`。

**T6：发布包构建与 SHA256 校验 —— ✅ 已完成（2026-09-19 校验）**

- 已产出：`/opt/service-hub/src/dist/service-hub-1.0.0-linux-amd64.tar.gz`
  （`111,117,172` 字节，gzip 完整）
- 外层 SHA256：`d8ef5a188e9b9de58b2f2cd06f5abb7335800b008df886c2716c666258f7d340`
- 解包后 `sha256sum -c SHA256SUMS` → **9 项全部 OK**（镜像 tar ×2、compose、脚本 ×4、hubctl、README）
- 已回填 `服务器部署实施清单.md` §2.6；发布包**不含** `.pypkg`。

**T7：修正过时文档（D-9）—— ✅ 已完成**

- D-9 原文两处（socket 组身份、compose 服务数量）早前已修正；
- 本轮又修掉 3 处同类残留（`:80` / `:87` / `:255` 的「三个受管进程」→ 六个）。

**T8：服务器临时物清理 —— 🟡 大部分已完成**

- **已消失**：`service-test/`（239 MB）、`src.old/`、`service-hub-src.tar`、一次性日志。
- **仍剩**：`/opt/service-hub/install_plugin.py`（4 KB）、`logs/*.log` 少量。
- **务必保留**：`data/`（1.1 GB）、`certs/`、`plugin-out/`（227 MB）、`src/`、
  `fixtures/sample.nc`（152 MB 真实 NC 样本）、`.itest-venv` / `.gate-venv`。

### 2.3 P2 —— 已登记技术债（不阻塞，按需排期）

| 编号 | 问题 | 备注 |
|---|---|---|
| V-2 | `list_files` 无 owner 过滤 | 需先定「共享池/私有」产品语义 |
| H-4 | SSE 长连接占线程与 DB 连接 | 压测才能暴露 |
| H-5 | 启动即 `alembic upgrade head`，无降级保护 | 升级回滚必须连数据目录 |
| D-3 / D-4 | 列表无分页 / `list_jobs` N+1 | 数据量上来才明显 |
| D-8 | healthcheck 探针 root 跑且吞异常 | 排障时看不到真实原因 |
| D-16 / D-24 | 仅规避未根治 | D-24 = 宿主缺 `python3.12-venv`，已踩两次 |
| T15 | 双运行时 NC→SHP 真实验收 | 🟡 **样本已有**（`fixtures/sample.nc` 152 MB）；**缺 `-conda.pypkg`**（需 Linux 装 conda/micromamba 构建） |

---

## 三、服务器连接信息与部署布局

### 3.1 连接方式

| 项 | 值 |
|---|---|
| 主机 | `${HUB_HOST}`（阿里云，Ubuntu 24.04，2 核 3.4 G，40 G 盘） |
| SSH | 端口 22，密钥认证。私钥在**本机** `D:\LearningMaterials\GIS+AI\service-hub\.deploy-tools\`（gitignore，勿外传） |
| 免交互工具 | 本机运行 `.deploy-tools/hubssh.py run "<命令>"`（paramiko，示例见下） |
| 管理员登录 | 用户 `admin`。**注意：`/opt/service-hub/data/bootstrap-admin.json` 现已被删除**（改密后按文档规范清理），旧口令已失效（探针返回 401）→ **需向运维索取当前口令**，或授权在服务器上重置 |

```bash
# 本机任意目录可执行（Windows Git Bash）
cd /d/LearningMaterials/GIS+AI/service-hub
C:/Users/Administrator/.workbuddy/binaries/python/envs/default/Scripts/python.exe \
  .deploy-tools/hubssh.py run "docker ps"
```

### 3.2 服务器目录布局（`/opt/service-hub/`）

| 路径 | 内容 | 状态 |
|---|---|---|
| `src/` | 部署源码（git archive 上传 + 后续单文件同步） | ✅ 与本地分支一致（含 D-25 修复） |
| `compose.override.yaml` | 宿主 override（edge 服务、端口、证书挂载注入） | ✅ 生效中 |
| `data/` | **业务数据**：SQLite（`db/hub.db`）、bootstrap 凭据、secrets、运行时目录 | 🔒 勿动 |
| `certs/` | TLS 证书（CN=${HUB_HOST}，2028-12 到期） | 🔒 勿动 |
| `nginx/` | edge 的 nginx.conf | ✅ 挂载给 edge |
| `plugin-out/` | `.pypkg` 插件包（227 MB） | 保留 |
| `logs/` | 集成测试/构建日志 | 可清 |
| `service-test/`、`src.old/`、`*.tar`、`/tmp/probe_*.py` | 历史临时物（约 245 MB） | 待清（T8） |
| `.itest-venv/`、`.gate-venv/` | 集成测试 / 质量门 venv | 保留（后续验收用） |

### 3.3 网络/端口总览

| 端口 | 用途 | 暴露范围 |
|---|---|---|
| 8443 | edge TLS → web/api | **公网**（云安全组放行，已实测） |
| 18000 | 后端 API 直连 | 仅服务器本机回环 |
| 18080 | web 控制台 | 仅服务器本机回环 |
| 80 / 443 | **另一容器 `cs-nginx`（非本项目）** | 公网，勿动 |

### 3.4 环境约束（踩坑记录，操作前必读）

- 镜像源：只有 `docker.m.daocloud.io` 可用；**不要改用户 `daemon.json`**。
- 默认 PyPI / npm / Debian 源都很慢（22 kB/s / 298 kB/s / 约 350 s 每包）。
  `build.sh` 已固定传 `APT_MIRROR` / `PIP_INDEX_URL` / `NPM_REGISTRY`（阿里云 + npmmirror），
  冷构建约 9 分钟；详见《服务器部署实施清单》§3.1.1。
- 容器内默认**没有 pytest**（镜像只装运行时依赖），容器内跑集成测试前需先装；
  `run_itests.py` 已把这一步固化进命令。
- 宿主机建 venv 前必须 `apt install python3.12-venv`（缺 `ensurepip`，D-24，已踩两次）。
- 服务器时间 2026-09-19；时区操作时注意日志时间戳为 GMT+8。
- 本机沙箱**禁止 SSH 转发到 18000**（19123/28000 可用）——要直连后端调试时，
  在服务器本机 `curl 127.0.0.1:18000` 或经 8443 外网走。
- 本机沙箱内 **Chrome 无法启动**（agent-browser 报 exit code 3），浏览器级走查需在
  运维本机执行。

---

## 四、建议的下一步（按优先级）

1. **（唯一遗留）人工浏览器走查**：`https://${HUB_HOST}:8443` —— 由运维在本机浏览器
   登录（admin / 当前口令）后，依次看 概览 / 文件（可试 >10 MB 文件走分块上传）/
   插件 / 作业 / 运行记录 / **调度页 cron 表单** 一次确认渲染与交互。
2. **T15 双运行时真实验收**（可选）：需先装 conda/micromamba 产出 conda 插件包；
   NC 样本 `fixtures/sample.nc`（152 MB）已就位。
3. **清理备份**（可选，确认运行稳定一周后）：`/opt/service-hub/data-backup-pre-t16.tar.gz`
   与 `data-cold-20260919` 共约 2 GB，是本次升级前的安全网。
4. 其余任务（T1~T8、T16、T17）**本轮已全部完成**，详见《完全部署剩余任务清单》§七。
