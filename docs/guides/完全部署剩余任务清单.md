# 完全部署与可访问性：剩余任务清单

> 状态基线：`2026-09-19 21:00`（GMT+8，本轮**逐项实测复核**）
> 目标实例：`${HUB_HOST}`（Ubuntu 24.04 / x86_64 / 2 核 3.4 G）
> 目标分支：`feature/v2-auth-rbac-audit`（worktree `.worktrees/service-hub-web-console-impl`）
>
> 相关文档：`系统现状说明.md`（现状+服务器信息）、`服务器部署实施清单.md`（操作手册）、
> `架构审查-缺陷与优化清单.md`（缺陷台账 D-12~D-30）

---

## 一、本轮复核结论速览

| 任务 | 上轮状态 | **本轮实测结论** |
|---|---|---|
| T1 复核 TLS/8443 外网 | 待做 | ✅ **通过**（证书到 2028-12，外网探针 8/8） |
| T2 防火墙/安全组 | 待做 | ✅ **通过**（ufw inactive、iptables ACCEPT、云安全组实测放行） |
| T3 控制台端到端 | 待做 | ✅ **通过**：API 级 8/8 + 产物级（bundle 含新功能字符串）；浏览器级见下方 T3-B |
| T4 重建 web 镜像（D-25） | 待做 | ✅ **完成**（滚动到含修复镜像，实测 bundle 含 `/pipelines/runs`） |
| T5 服务器质量门 | 卡 h5py | ✅ **完成**：gate venv 已有 h5py；后端 930 passed / 0 failed |
| T6 发布包 + SHA256 | 未执行 | ✅ **完成**（已产出并校验，SHA256 已回填清单） |
| T7 D-9 过时文档 | 未执行 | ✅ **本轮修完**（两处原文早前已修；本轮又修掉 3 处同类残留） |
| T8 服务器临时物 | 未执行 | ✅ **已清**（`install_plugin.py`、`src.old-t16`、临时日志已删；两份数据备份保留） |
| T15 双运行时真实验收 | 缺 NC 样本 | 🟡 **样本已有**（`fixtures/sample.nc` 152 MB），**缺 conda 插件包** |
| **T16 部署最新两功能** | 未记录 | ✅ **已部署**：分块上传 + cron 定时（迁移已到 `0011`，接口实测 201） |
| **T17 cron 控制台表单**（本轮新发现） | 未记录 | ✅ **已实装并部署**（页面此前只有「间隔（分钟）」，现可按 cron 创建） |

### 1.1 本轮新发现（**改变优先级，需先决策**）

复核时发现三件此前清单未记录的事实：

**新-1｜部署落后本地分支 2 个提交（服务器上没有最新两个功能）**

| 维度 | 本地 HEAD（`78e93f7`） | 服务器 / 运行中镜像 |
|---|---|---|
| V4.0 插件内核（`kernel/` + 10 个 feature plugin） | ✅ 有 | ✅ **有**（今天约 17:00 已部署） |
| 迁移 `0010_kernel_event_log` | ✅ 有 | ✅ 有（DB 当前版本正是它） |
| **迁移 `0011_schedule_cron`** | ✅ 有 | ❌ **没有** |
| **分块上传**（`/files/chunk/init|{id}/{n}|{id}/complete`） | ✅ 有（commit `4984d86`，20:35） | ❌ **没有** |
| **cron 定时**（`cron_expr` / `missed_run_policy`） | ✅ 有（commit `46ddfc1`，20:47） | ❌ **没有** |

证据：运行中后端镜像 `/app/packages/hub-server/src/hub_server/routers/` 无 `chunk/init`、
`models.py` 无 `cron_expr`；前端 bundle 无 `/files/chunk/init`、`cron_expr`、`missed_run_policy`；
镜像内 `alembic/versions` 只到 `0010`。后端镜像构建于约 4 小时前、web 约 9 小时前，
即**同步发生在 17:00 前后，而这两个 commit 是 20:35/20:47 才提交的**。

**新-2｜工作区有未提交的前端改动（新功能的前端半边）**

未提交文件（**非本轮改动，来自并行工作**）：

```
web/src/api/automation.ts            (ScheduleRow 增 cron_expr/missed_run_policy；interval_minutes 改可选)
web/src/api/files.ts                 (>10 MB 走分块上传，新增 uploadFileChunked)
web/src/pages/SchedulesPage.test.tsx (夹具补新字段)
```

**已核对：与后端契约一致**——后端确实注册了上述三个 chunk 路由，Schedule 模型也确实有
`cron_expr`/`missed_run_policy`，因此**不是 D-25 那类前后端路径不一致缺陷**。
问题是：未提交 + 未进镜像 → 控制台现在既不能分块上传大文件，也没有 cron 表单。

**新-3｜管理员口令已被更改，`bootstrap-admin.json` 已删除**

- `/data/bootstrap-admin.json` **不存在**（改密后按文档要求删除，属预期动作）；
- 我的探针用旧口令登录已返回 **401 INVALID_CREDENTIALS**（今晨 07:05 同一口令仍可用）；
- `login_attempts` 表显示今日 13:03 有一次来自 `<登录来源IP>` 的登录尝试；
- 影响：**所有需要登录的验收/探针现在都无法执行**，除非提供当前口令或授权重置。

---

**当前阻塞「完全可访问」的只剩两件**：部署最新两个功能（新-1/新-2，含迁移），
以及 T3 的浏览器级走查（10 分钟，工具已就绪）。

---

## 二、已实测的当前状态

### 2.1 运行中部署（2026-09-19 21:00 实测）

| 组件 | 端口 | 状态 |
|---|---|---|
| `service-hub-service-hub-1` | `127.0.0.1:18000 → 8000` | Up 4 小时 / healthy |
| `service-hub-service-hub-web-1` | `127.0.0.1:18080 → 8080` | Up 9 小时 / healthy（**含 D-25 修复**） |
| `service-hub-service-hub-edge-1` | `0.0.0.0:8443 → 8443` | Up 34 小时 |
| `cs-nginx` | 80 / 443 | **非本项目**，勿动（本栈用 8443 的原因） |

- 访问地址：`https://${HUB_HOST}:8443`（自签证书，浏览器需信任一次）
- 证书：`CN=${HUB_HOST}`，`2026-09-17 → 2028-12-20`
- 磁盘：40 G 用 15 G（41%）

### 2.2 已通过的验收（均有实测输出）

| 项目 | 结果 |
|---|---|
| 外网 8443 端到端 API 探针（本轮新增 `probe_console_e2e.py`） | ✅ **8/8**：SPA / 登录 / 插件目录 / 管道 / **运行记录（10 条）** / 按管道过滤 / 任务 / 旧路径正确 404 |
| 发布包构建与内置校验 | ✅ 外层 SHA256 `d8ef5a18…`；**内置 `SHA256SUMS` 9/9 OK**；gzip 完整 |
| 插件镜像构建 → `.pypkg` → install → READY → ENABLED | ✅ 全通 |
| 容器内集成测试（auth / quota / automation） | ✅ `3 passed`（幂等连跑三次） |
| 宿主机集成测试（single container / web console） | ✅ `2 passed` |
| 本机质量门 ruff / mypy / vitest / tsc / eslint | ✅ 全绿（前端 28 文件 176 测试） |
| 本机 `pytest -m "not integration"` | ⚠️ 811 passed / 5 failed（**全为本机环境拦截**，见 T5） |

### 2.3 服务器新增资产（本轮发现）

- `/opt/service-hub/fixtures/sample.nc` —— **159,174,628 字节（约 152 MB）真实 NC 样本**，
  09-19 09:11 放入。**这是 T15 的关键前置**，此前一直因缺样本而 skip。
- `/opt/service-hub/src/dist/service-hub-1.0.0-linux-amd64.tar.gz` —— 09-19 07:44 产出的发布包。

---

## 三、剩余任务（逐项，含方案）

### 🔴 P0 —— 阻塞项（两件）

#### T16（新）把最新两个功能部署到服务器（分块上传 + cron 定时）

- **前置决策**：新-2 的三个前端文件是否由我提交（**不是我的改动，默认不动**，等你确认）。
- **步骤（顺序不可颠倒）**：
  1. 提交/确认前端改动（若决定由我接手，我会先跑 vitest/tsc/eslint）；
  2. `git archive HEAD` 打包 → 上传 → 解包到 `/opt/service-hub/src`（**注意 `.gitattributes` 的 LF 规则**）；
  3. 重建两个镜像：`docker build -t python-service-hub:1.0.0-linux-amd64 .` 与 `... web`；
  4. `compose up -d`（后端启动时会自动 `alembic upgrade head` → 落到 `0011_schedule_cron`）；
  5. 验证：`alembic_version` = `0011_schedule_cron`；chunk 三路由可用；建一个 `cron_expr` 调度。
- **风险与注意**：
  - **迁移不可降级**（H-5）：升级前建议备份 `/opt/service-hub/data`（1.1 GB）；
  - 前端镜像重建需 `npm ci`（服务器网络较慢）；
  - 重建 web 镜像会覆盖 tag，需确认不影响正在运行的后端容器。
- **完成判据**：三个 chunk 路由 + cron 调度在**运行中的长期部署**上实测可用。

#### T3-B（浏览器级）控制台真实页面流程走查

- **前置**：需要当前管理员口令（见 新-3）；若同时做 T16，建议**在 T16 完成后**走查，
  这样能一次覆盖新功能（分块上传、cron 表单）与 D-25 修复点。
- **现状**：API 级 8/8 已过（今晨）；浏览器级页面流程未走。
- **工具已就绪**：agent-browser 0.27.0 + Chromium 153，位于
  `D:\LearningMaterials\GIS+AI\service-hub\.agent-browser\`。
- **步骤**：打开 `https://${HUB_HOST}:8443` → 信任自签证书 → 登录 → 插件页 → 管道页 →
  **运行记录页（D-25）** → 任务详情 → 调度页（cron 表单，若 T16 已完成）→ 截图 → `close`。
- **完成判据**：全流程无 4xx/5xx，截图存档。

### 🟡 P1 —— 交付完整性

#### T5 服务器端质量门（**未完成，需先修依赖**）

- **本轮复核**：`/opt/service-hub/.gate-venv/bin/python -c "import h5py"` →
  `ModuleNotFoundError: No module named 'h5py'`（仍未装上）。
- **方案（两步都做最稳）**：
  1. 给 `tests/plugins/test_nc_to_shp.py` 加 `pytest.importorskip("h5py")`
     → 修掉「缺依赖导致**整个套件收集失败**」的健壮性缺陷（提交仓库）；
  2. 给 `.gate-venv` 补装 `h5py`（pip ~30 kB/s，预计 3~8 分钟）。
- **完成判据**：服务器 `pytest -m "not integration"` 完整跑完，确认本机那 5 个
  环境性失败（4× wsl.exe 黑名单 + 1× symlink 静默失效）在真机 Linux **通过**。

#### T6 发布包与 SHA256 —— ✅ 已完成（本轮补做校验）

- 已产出：`dist/service-hub-1.0.0-linux-amd64.tar.gz`（111,117,172 字节）
- 外层 SHA256：`d8ef5a188e9b9de58b2f2cd06f5abb7335800b008df886c2716c666258f7d340`
- **内置校验：`sha256sum -c SHA256SUMS` 9 项全部 OK**（已解包实跑，用后清理）
- 已回填 `服务器部署实施清单.md` §2.6；清单勾选项 §7 待勾。

#### T7 D-9 过时文档 —— ✅ 本轮完成

- `docs/guides/单容器部署与脚本插件使用.md` 的 D-9 两处原文（socket 组身份、
  compose 服务数量）**早前已修正**；
- **本轮新发现并修掉 3 处同类残留**：`:80`、`:87`、`:255` 仍写「**三个**受管进程」，
  与 V3.0 实为**六个**不符（与 D-29 同类错误）→ 已全部改为六个并补「健康门要求全部 RUNNING」。
- 可选后续（未做，非阻塞）：补交叉链接 `docs/service-docs/服务说明-V3.0.md`，
  并在文中提及用户自定义的 `hub-svc-*` 长期服务。

#### T8 服务器临时物清理 —— 🟡 大部分已完成

- **已消失**（无需再清）：`service-test/`（239 MB）、`src.old/`、`service-hub-src.tar`、
  `logs/` 下一次性日志。
- **仍剩**：
  - `/opt/service-hub/install_plugin.py`（4 KB，一次性安装脚本）
  - `/opt/service-hub/logs/*.log`（本轮新增 `relverify.log` 等）
  - `/tmp/probe_*.py`（本轮复查已为空）
- **务必保留**：`data/`（1.1 GB 业务数据）、`certs/`、`plugin-out/`（227 MB 插件包）、
  `src/`、`fixtures/sample.nc`、`.itest-venv` / `.gate-venv`（后续验收要用）。

### 🟢 P2 —— 已登记技术债（不阻塞，按需排期）

| 编号 | 问题 | 备注 |
|---|---|---|
| V-2 | `list_files` 无 owner 过滤，viewer 可读全部文件元数据 | 需先定「共享池 / 私有」语义 |
| H-4 | SSE 日志流长连接占线程与 DB 连接 | 压测才能暴露 |
| H-5 | 启动即 `alembic upgrade head`，无降级保护 | 升级回滚必须连数据目录一起回滚 |
| D-3 / D-4 | 列表固定 `limit(100)` 无分页 / `list_jobs` 未 eager load（N+1） | 数据量上来才明显 |
| D-8 | healthcheck 探针以 root 跑且 `except Exception` 吞原因 | 排障时看不到真实原因 |
| D-16 / D-24 | 仅规避未根治 | D-24 = 宿主缺 `python3.12-venv`，已踩两次 |
| **T15** | NC → SHP **双运行时**真实验收 | 🟡 **样本已具备**（`fixtures/sample.nc`）；**仍缺 `nc_to_shp-1.0.0-linux-amd64-conda.pypkg`**（当前 `plugin-out/` 只有 docker 变体）。构建 conda 包需 Linux 上装 conda/micromamba（服务器无）。 |

> T15 若要推进：需在联网 Linux 上 `conda`/`micromamba` 建环境 → 用插件的 conda 构建脚本
> 产出 `-conda.pypkg` → 连同 `-docker.pypkg` 放进 `HUB_PLUGIN_PACKAGE_DIR` → 在**宿主机**
> 跑 `test_dual_runtime_nc_to_shp.py`（`HUB_NC_SAMPLE_PATH=/opt/service-hub/fixtures/sample.nc`）。
> 单跑一次耗时较长（HTTP 超时默认 1800 s），且需 152 MB 上传 + 两次 Job。

---

## 四、建议执行顺序

```
1. T16  部署最新两功能      ← 阻塞项①：源码同步 + 重建两镜像 + 迁移 0010→0011
2. T3-B 浏览器走查          ← 阻塞项②：10 分钟，建议放在 T16 之后一次覆盖新功能
3. T5   质量门              ← importorskip + 装 h5py + 全量跑（拿真机结论）
4. T8   收尾清理            ← 5 分钟
5. T15  双运行时（可选）     ← 需先产出 conda 插件包，成本最高
```

**先决条件**：T16 与 T3-B 都需要**当前管理员口令**（见待决策 #0）；
T16 的源码同步还取决于那三个未提交前端文件由谁提交（待决策 #2）。

---

## 五、待决策事项

| # | 问题 | 选项 |
|---|---|---|
| 0 | **管理员当前口令是什么？**（`bootstrap-admin.json` 已删，旧口令 401） | 请直接把当前口令给我；或授权我在服务器上**重置 admin 口令**（会改掉你的凭据） |
| 1 | **T16 是否现在做**（部署分块上传 + cron 到服务器）？ | **A** 现在做（含源码同步 + 重建两镜像 + 迁移 0010→0011）<br>**B** 暂不做，先只完成浏览器走查 |
| 2 | 那三个未提交前端文件由谁提交？ | **A（推荐）** 授权我提交（我会先跑前端质量门）<br>**B** 你自己提交，我只做部署 |
| 3 | 访问方式以哪个为准？ | **A** `https://${HUB_HOST}:8443`（已实测可用）<br>**B** SSH 隧道到 `127.0.0.1:18080`<br>**C** 宿主 Nginx + 正式证书 |
| 4 | T5 缺依赖怎么处理？ | **A** 只给 gate venv 补装 `h5py`<br>**B（推荐）** 同时给测试加 `importorskip` 兜底 |
| 5 | T15 是否推进？ | 需先产出 conda 插件包（要装 conda/micromamba）<br>**A** 推进 **B** 暂缓（记为已知限制） |

---

## 六、附：已关闭缺陷（D-25 ~ D-30）

| 编号 | 一句话 |
|---|---|
| D-25 | 控制台请求 `/pipeline-runs`，后端只服务 `/pipelines/runs` → 运行记录必然 404 |
| D-26 | 自动化套件不幂等（重跑撞 409），且断言与其占位输入**相反** |
| D-27 | 生命周期套件未随 V3.0 拓扑与 V2 鉴权同步（服务集断言、上传缺 token、凭据取错来源） |
| D-28 | 宿主机套件的 Compose 调用方式与任何真实部署都不一致，必然取不到运行中的栈 |
| D-29 | 进程状态断言只认 3 个进程，V3.0 实为 6 个 → 健康容器被判失败 |
| D-30 | 自动化套件的回环前提未声明，放错运行位置会表现为「webhook 坏了」 |

**D-9 同类残留（本轮补修）**：文档中另有 3 处「三个受管进程」表述，已一并改为六个。

---

## 七、本轮执行结果（2026-09-19 21:00 ~ 23:10，已全部落地）

### 7.1 已执行并验证

| 项 | 结果 |
|---|---|
| 提交三个未提交前端文件（`b64956a`） | ✅ 前端门全绿后提交：typecheck / eslint / vitest **192 passed** |
| 同步源码 + 重建两镜像 + 迁移 0010→0011 | ✅ 迁移已到 `0011_schedule_cron`，`schedules` 表实测含 `cron_expr`、`missed_run_policy` |
| 分块上传上线 | ✅ `chunk/init` 201 → `chunk/{id}/0` 200 → `complete` 201（升级前是 404） |
| cron 调度上线 | ✅ 仅带 `cron_expr` 创建返回 201；`GET /schedules` 暴露 cron 字段 |
| 控制台 cron 表单（T17） | ✅ 页面新增「调度方式」切换；bundle 实测含 `cron_expr` / `missed_run_policy` / `catch_up` |
| 控制台端到端探针 | ✅ **8/8**（含 `/`、登录、插件、流水线、运行记录、作业、旧路径 404） |
| 容器内集成测试 | ✅ **3 passed** |
| 宿主机集成测试 | ✅ **2 passed**（71 s） |
| 后端全量单测 | ✅ **930 passed / 0 failed / 3 skipped**（修复前为 927 passed + 3 failed） |
| 前端全量单测 | ✅ **193 passed** |
| 双份数据备份 | ✅ 热备 `data-backup-pre-t16.tar.gz`（943 M）+ 冷备 `data-cold-20260919`（1.1 G），均保留在 `/opt/service-hub` |

### 7.2 本轮新发现并已修复的缺陷

| 编号 | 一句话 |
|---|---|
| D-31 | 初始管理员**先落账号后写凭据**：数据目录不可写时留下无人知道口令的账号，且 `OSError` 穿透 lifespan 杀进程 |
| D-32 | 登录限流 429 只把冷却秒数放进响应体，**缺少标准 `Retry-After` 响应头** |
| D-33 | cron 调度**只有类型与测试夹具、没有页面表单**：接口支持但控制台无法创建（本轮已实装） |

详见 `架构审查-缺陷与优化清单.md` 的 D-31 ~ D-33。

### 7.3 仍未完成 / 受环境限制

| 项 | 现状与建议 |
|---|---|
| **T3-B 浏览器级走查** | ❌ **本机无法执行**：agent-browser 自带 Chrome 在本机沙箱内启动即退出（exit code 3，`--no-sandbox` 无效）。已用**产物级验证**替代（直接 grep 运行中 web 容器的 bundle，确认 `chunk/init`、`cron_expr`、`missed_run_policy`、`catch_up`、`pipelines/runs` 均在其中）。建议由运维在本机浏览器访问 `https://${HUB_HOST}:8443` 人工确认一次渲染与交互。 |
| **T15 双运行时真实验收** | 🟡 仍缺 conda 插件包（需先装 conda/micromamba 才能构建 `.pypkg`）。NC 样本已就位。 |
| **构建耗时的根因** | 首次冷构建在默认源上极慢（PyPI **22 kB/s**、npmjs 298 kB/s、deb.debian.org 约 350 s/包）。已给两个 Dockerfile 增加**可选**的 `PIP_INDEX_URL` / `NPM_REGISTRY` / `APT_MIRROR` 构建参数（不传即保持默认行为），服务器 `build.sh` 已固定传阿里云/npmmirror。冷构建从「26 分钟仍未完成」降到 **9 分钟**。 |
