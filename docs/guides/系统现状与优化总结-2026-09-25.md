# service-hub 系统现状与优化总结

> 生成时间：`2026-09-25 11:0x`（GMT+8）
> 覆盖范围：本轮任务（GitHub 化 + CI/CD 接入 + 服务器自动部署上线）的全部成果，
> 以及在此之后仍然存在的问题与优化建议。
> 线上实例：`${HUB_HOST}`（Ubuntu 24.04 / x86_64 / 2 核 3.4 G），入口 `https://${HUB_HOST}:8443`
> 关联文档：`系统现状与交付流水线优化方案-2026-09-25.md`（优化台账）、
> `CI-CD-GitHub流水线说明.md`（流水线操作手册）、`完全部署剩余任务清单.md`（历史任务追踪）

---

## 一、本轮做了什么

| # | 事项 | 结果 |
|---|---|---|
| 1 | 建远端仓库并推送 `main` | ✅ `github.com/wuxiaolei-star/service-hub`（**私有**），434 个文件，历史未改写 |
| 2 | 敏感信息处置 | ✅ 明文口令/私钥/本地产物全部排除上传，另加提交门禁 `deploy/ci/check_secrets.py` |
| 3 | CI 质量门搬到 GitHub Actions | ✅ 密钥扫描 / 后端 ruff+mypy+pytest / 前端 lint+tsc+vitest+build / 主镜像 buildx |
| 4 | CD 落地（服务器主动拉取） | ✅ systemd timer 每 3 分钟比对远端 tip，有差异才部署，服务器零入站端口 |
| 5 | 服务器源码转 git 检出 | ✅ 备份 `src.pre-git-20260925` 后 clone，14 个 CI 文件补齐、3 个文件更新 |
| 6 | 首次流水线部署上线 | ✅ `3f8a9b3`，157 s，三容器 healthy，api/web/edge 全 200 |
| 7 | 修流水线 5 个缺陷 | ✅ 见 §三（含一个导致**自动部署全程失效**的严重缺陷） |
| 8 | 打包/构建提速（上一阶段） | ✅ 插件打包 13~15 min → 144 s；插件冷构建 25 min → 188 s |

---

## 二、当前系统现状（均为实测数据）

### 2.1 代码仓库

| 项 | 值 |
|---|---|
| 远端 | `git@github.com:wuxiaolei-star/service-hub.git`（私有） |
| 分支 / 提交 | `main` @ `a2a0e00`（本轮最后一条：自动部署修复） |
| 跟踪文件 | 434 |
| 本机推送 | `~/.ssh/id_ed25519_github`（账户 SSH keys，Authentication） |
| 服务器拉取 | `~/.ssh/id_ed25519_service_hub_deploy`（仓库 Deploy keys，**只读**） |

### 2.2 运行时

| 组件 | 容器 | 状态 | 端口 |
|---|---|---|---|
| hub-api | `service-hub-service-hub-1` | healthy | `127.0.0.1:18000` |
| web 控制台 | `service-hub-service-hub-web-1` | healthy | `127.0.0.1:18080` |
| edge TLS | `service-hub-service-hub-edge-1` | Up | `0.0.0.0:8443`（唯一对外端口） |

- `system/info`：`hub_version 0.1.0` / `python 3.12` / `deployment_mode offline`，health `UP`
- 资源：磁盘 40 G 用 22 G（剩 17 G），内存 3.4 G 用 1.1 G
- 服务器上另有**非本项目**容器 `cs-nginx`（占用 80/443，Up 5 周）与 `peaceful_cori`，
  不属于 service-hub 栈，清理前需先确认归属

### 2.3 业务运行数据

| 项 | 现状 |
|---|---|
| 插件 | `nc_to_shp`：`1.0.1` **ENABLED**（真实样本链路可用）；`1.0.0` 亦为 ENABLED（历史版本，已无启用调度引用） |
| 调度 | 3 条：`id=3 nc-to-shp-hourly-real-nc-101`（1.0.1，**enabled**，每小时）正常；`id=1/2`（1.0.0）已 disabled |
| 作业总量 | 189 |
| 近 7 日成功率 | 09-19 `1/27` → 09-23 `0/24` → **09-24 `10/26`** → **09-25 `3/3`（100%）** |
| 最近 8 个作业 | 全部 `SUCCESS`（`nc_to_shp@1.0.1`） |

> 09-20~09-23 的 0 成功率是占位 NC 文件（10 字节）时期的历史数据；
> 换用真实样本 + 插件加输入预校验后，09-25 已回到 100%。
> **注意：当前样本仍是合成 NC（12 点/8 单元/20 时刻），仅证明链路通，不是真实业务数据。**

### 2.4 交付链路

```
git push → GitHub Actions（质量门：密钥扫描/后端/前端/镜像构建）
                                  ↓（仅记录，不触碰服务器）
服务器 systemd timer（每 3 min）→ pull-deploy.sh 比对远端 tip
                                  ↓ 有差异
        checkout → 质量门(可选) → 双备份 → build.sh → compose up
                                  ↓
        健康检查(20 次×10 s) → 走查 → 记录 releases.log + 备份轮换
                                  ↓ 任一阶段失败
                       自动回滚（冷备恢复 + 重建 + 重启）
```

| 环节 | 实测耗时 |
|---|---|
| GitHub Actions 全量 | 未确认（私有仓库日志本机不可见，**待你核对**） |
| 服务器容器内全量 pytest | **481 s**（空载）/ **706 s**（部署期间实测，953 passed / 2 skipped）——2 核机器上波动很大 |
| 完整流水线（带质量门） | 579 s ~ **805 s**（两次实测） |
| 流水线 `--skip-gate` | **157 s** |
| 轻量路径（文档改动） | **0 s，零中断**（已验证） |
| 主镜像构建 | 冷 26 min（默认源）/ 9 min（镜像源）/ **温缓存 4.5 min** |
| 插件打包 | 13~15 min → **144 s**（zstd 19→10 + 流式压缩后） |
| 插件镜像冷构建 | ~25 min → **188 s**（镜像源参数化后，实测腾讯云最快） |

### 2.5 敏感信息

| 敏感项 | 本地位置 | 入库 | 运行期来源 |
|---|---|---|---|
| admin 口令 | `.deploy-tools/*.py`、`登录账号密码.txt` | ❌ gitignore | 首启生成 `data/bootstrap-admin.json`（用完即删） |
| 服务器 SSH 私钥 | `.deploy-tools/id_ed25519`、`ssh_env.json` | ❌ | paramiko 本机读取 |
| GitHub 凭证 | 本机 `~/.ssh`、服务器 `~/.ssh` | ❌ | Actions 只用内置 `GITHUB_TOKEN` |
| 本地产物 / `.workbuddy` / `.agent-browser` | 项目根 | ❌ | — |
| 服务器 IP | docs 约 20 处 | ✅ 私有可接受 | 转公开前需 `sed 's/39\.96\.194\.156/${HUB_HOST}/g'` |

已跟踪文档中**无明文口令**（仅有 `<当前 admin 口令>` 占位符）。
`check_secrets.py` 实测本仓库：0 error / 11 warning（warning 为疑似赋值与公网 IP，不阻断）。

---

## 三、本轮发现并已修复的问题

| # | 问题 | 根因 | 修复 |
|---|---|---|---|
| 1 | 质量门"失败"但 pytest 没跑 | 镜像 `ENTRYPOINT=service-hub-entrypoint` 先自举，缺 `HUB_HOST_DATA_DIR` 直接崩 | `docker run --entrypoint bash` |
| 2 | 用例 `test_nested_dockerfiles_...` FileNotFoundError | 运行时镜像不含 `git`，用例依赖 `git check-attr` | 加缺失即跳过（Actions 上有 git，保护仍在） |
| 3 | "备份失败并回滚"（实际容器没停） | `compose.yaml` 用 `${HUB_HOST_DATA_DIR:?}`，流水线没 export | pipeline 补 `export HUB_HOST_DATA_DIR` |
| 4 | 改了流水线却仍在跑旧逻辑 | bash 按字节偏移读运行中的脚本，checkout 后新旧混杂 | `pull-deploy.sh` 先 checkout 再 exec |
| 5 | **自动部署全程失败**（`status=1/FAILURE`） | `set -u` 下 `PIPELINE_ARGS` 未定义即退出；手动跑都带了该变量所以没暴露 | 改 `${PIPELINE_ARGS:-}` |
| 6 | 改一篇文档就触发 10 分钟全量部署 + 1~2 分钟中断 | 未区分改动范围 | 新增轻量路径：改动仅落在 `docs/*`、`*.md`、`README*`、`LICENSE`、`.git*` 时只记录发布不重建（主镜像 Dockerfile **不 COPY 这些路径**，故镜像内容等价） |

| 7 | 轻量路径对中文文档不生效（仍走 10 分钟全量） | git 默认把非 ASCII 路径输出为八进制转义（`"docs/guides/\347\263\273...md"`），匹配不上 `docs/*` | `git diff` 加 `-c core.quotePath=false` |
| 8 | 定时器与手动部署重叠时 systemd 记 `Failed unit` | 并发锁按失败码退出（1），实际是"已有部署在跑"的正常情况 | pipeline 锁冲突退 3；pull-deploy 收到 3 视为空操作；单元加 `SuccessExitStatus=0 2 3` |

> 第 5 条是最严重的一条：修复前 timer 每次触发都以失败告终，CD 实际处于"装好但不工作"的状态。
> 第 6 条同时刷新了 `$REMOTE_DIR/deploy/ci/` 稳定副本——timer 执行的是它，否则脚本修复要等下次 `install-poller.sh` 才生效。
> 第 7、8 条是在修复后首次自动部署时暴露的：前者让轻量路径形同虚设，后者制造假故障告警。

**自动链路端到端验证（11:12~11:26）**：timer 自动触发 → 全量部署 `6a15b32`（质量门 953 passed / 706 s，
805 s 完成，三容器 healthy）；3 分钟后 timer 再次触发，`c72323f` 为纯文档改动 →
`docs-only change (1 files)` → `LIGHTWEIGHT DEPLOY FINISHED`，`elapsed=0s`，`result=SUCCESS-LIGHTWEIGHT`，
期间服务未重启、无中断。即：**改文档不再触发重建，改代码才重建，且全程无人干预。**

---

## 四、仍然存在、需要优化的问题

### P0 — 建议近期做（收益高、风险低）

> 2026-09-25 下午执行批注：O-1/O-2/O-3/O-4/O-6 已落地（见 §七），O-5 于同日服务器侧执行，
> O-7 仍需人工看一眼 Actions 页。

| # | 问题 | 影响 | 建议方案 | 涉及位置 |
|---|---|---|---|---|
| **O-1** | 镜像 tag 固定为 `python-service-hub:1.0.0-linux-amd64`，不含版本信息 | 回滚只能"用**当前**源码重建旧镜像"，代码回滚不精确，且耗时 4.5~9 min | 构建时按 commit 打 tag（`...:1.0.0-<sha>`）并保留 `previous` tag；回滚改为切 tag，不必重建 | `build.sh`、`deploy/ci/deploy-pipeline.sh` |
| **O-2** | 服务器侧质量门默认开启，每次部署固定 8 min | timer 部署总时长 ~10 min，中断窗口被拉长 | 默认 `--skip-gate`（质量门交给 Actions），保留开关；或在 Actions 通过后才打部署标记 | `deploy/ci/deploy-pipeline.sh` |
| **O-3** | 插件包每次都重新打包（即使源码未变） | 约 144 s/次 的无效开销 | 按内容哈希复用：`sha256(源码摘要 + 基础镜像 digest)` 命中即跳过 | `hub_publisher/`（原 P0-3） |
| **O-4** | 三个 Dockerfile 未用 BuildKit 缓存挂载 | 冷/温构建都慢 | `RUN --mount=type=cache` 缓存 apt/pip/npm，配 `DOCKER_BUILDKIT=1` | `Dockerfile`、`web/Dockerfile`、插件 Dockerfile（原 P0-6） |
| **O-5** | 线上同时存在 `nc_to_shp` 1.0.0 与 1.0.1 两份构建，1.0.0 仍是 ENABLED | 约 1 GB 镜像 + 一份 staging 数据；有误用旧版本风险 | 确认无调度引用后禁用并清理 1.0.0 构建，同时删掉 2 条 disabled 调度 | 线上 registry API（原 P2-3） |
| **O-6** | 部署失败/成功**无任何通知** | timer 失败只写在 journalctl，无人知晓 | 流水线结束回调站内 webhook 或邮件（失败必须告警） | `deploy/ci/deploy-pipeline.sh` |
| **O-7** | GitHub Actions 首跑结果未确认 | 质量门是否真绿未知 | 你看一眼 Actions 页，红了我照日志修 | — |

### P1 — 流程与数据

| # | 问题 | 建议 |
|---|---|---|
| **O-8** | 线上 NC 样本是**合成数据**，双运行时（conda）验收未完成 | 用真实业务 NC 替换，重跑 T15 双运行时验收（需你提供样本） |
| **O-9** | `webhooks.allow_private_networks: true` 为便于本机验收而开启 | 生产应改回 `false`，否则存在 SSRF 面 |
| **O-10** | 流水线 stage 7「走查」实际一直跳过（`walkthrough.sh` 不存在） | 补一个业务级验收脚本（登录 → 插件 → 跑一个作业），否则部署只能证明"进程活着" |
| **O-11** | 健康检查只探 200，无业务语义 | 增加 `/jobs` 或统计端点断言，或纳入走查 |
| **O-12** | 备份轮换保留 3 份（冷备约 489 M/份），无水位告警 | 加磁盘水位检查（<20% 告警/拒绝部署） |
| **O-13** | `compose down` → `up` 之间有 1~2 min 中断 | 蓝绿/滚动发布（P2-4），或至少把构建前置到 down 之前 |
| **O-14** | 服务器上 `cs-nginx`（80/443，Up 5 周）、`peaceful_cori` 归属不明 | 确认归属后再决定是否清理，勿误删 |

### P2 — 中长期

| # | 事项 | 收益 |
|---|---|---|
| O-15 | 引入镜像仓库（registry / ACR） | 省掉 1 GB 级 `docker save`+zstd+上传+`docker load` 四遍读写 |
| O-16 | 插件改用 conda-pack 运行时 | `env.tar.zst` 远小于 1 GB 完整镜像，是双运行时验收前置 |
| O-17 | 制品签名与校验前移到 CI | 现在只在服务端安装时校验 sha256 |
| O-18 | 4.3 i18n（已从批次计划移出） | — |
| O-19 | 前端新功能须 grep 真实构建产物校验（D-33） | 只改类型/夹具不会被任何质量门发现 |

---

## 五、关键指标：现状 vs 目标

| 指标 | 现状 | 目标 | 依赖 |
|---|---|---|---|
| 代码改动到上线（全自动） | ~10 min（质量门占 8 min） | **~3 min** | O-2 |
| 文档改动触发的"部署" | 轻量路径，秒级、零中断 | 已达成 | — |
| 回滚耗时 | 4.5~9 min（需重建镜像） | **<1 min**（切 tag） | O-1 |
| 插件打包 | 144 s | 源码未变时 0 s | O-3 |
| 主镜像冷构建 | 9 min（镜像源） | <5 min | O-4 |
| 调度作业成功率 | 100%（09-25，3/3） | 保持，并换真实样本 | O-8 |
| 部署失败可感知 | 无通知 | 必达告警 | O-6 |
| 磁盘可用 | 17 G | 加水位告警 | O-12 |

---

## 六、下一步建议（需你拍板的三件事）

1. **是否现在做 O-1 + O-2**（镜像按 commit 打 tag、质量门交给 Actions）——
   这两项把自动部署从"能跑"变成"好用"，改动集中在 `build.sh` 与流水线脚本，风险低，
   但 O-1 会改变镜像 tag，需要一次真实部署验证。
2. **提供一份真实业务 NC 样本**（O-8）——合成样本只能证明链路通，不能证明业务正确。
3. **顺手看一眼 GitHub Actions 首跑结果**（O-7）——私有仓库日志我这边看不到。

其余 P0 项（O-3/O-4/O-5/O-6）可按上表顺序推进，都是独立改动、互不依赖。

---

## 七、2026-09-25 下午执行记录（O-1/O-2/O-3/O-4/O-5/O-6/O-10/O-12）

核查结论先行：自动链路（push → Actions → timer 拉取部署 → 轻量路径）实测全部正常，
部署机三容器 healthy、磁盘 23 G/40 G、`.deployed-commit = origin/main = 1119248`。
另发现一个**新问题**并随本轮修复：原回滚逻辑是"恢复 data 后用 `/opt/service-hub/build.sh`
重建镜像"，而 stage 1 已经 checkout 到**新** commit——即旧回滚只回滚数据、代码仍是失败的
新代码。O-1 的 tag 化从根上解决了这一点。

| # | 改动 | 落点 |
|---|---|---|
| O-1 | 镜像按 commit 打 tag：`deploy/ci/build-images.sh`（入库，替代服务器本地 build.sh）产出 `:1.0.0-<sha>` 并移动 `:1.0.0-linux-amd64`；上一版保留为 `:rollback-target`；回滚 = 恢复冷备 + 重打移动 tag（秒级、且代码真正回到上一版）；成功后清理更早 commit tag + prune 悬空层 | `deploy/ci/build-images.sh`（新）、`deploy/ci/deploy-pipeline.sh` |
| O-2 | 服务器侧质量门默认关闭（`HUB_PIPELINE_GATE=0`，Actions 已全量跑）；`HUB_PIPELINE_GATE=1` 或 `--run-gate` 可手动补跑 | `deploy/ci/deploy-pipeline.sh` |
| O-3 | 插件包内容哈希缓存：键 = sha256(manifest + 源码摘要 + Dockerfile + 三个 hub 包源码树 + 基础镜像 ID + 镜像源参数 + zstd 等级 + arch)，命中且 sha256 校验通过即跳过 docker build/save/zstd/tar 全程；缓存目录默认 `<output>/.pypkg-cache`，`HUB_PLUGIN_CACHE_DIR` 可迁移、`HUB_PLUGIN_CACHE=0` 可关闭；仅保留最新 3 份。conda-pack 不缓存（依赖宿主机状态，无法用本地键钉住） | `packages/hub-publisher/src/hub_publisher/builder.py` + 8 个新测试 |
| O-4 | 三个 Dockerfile 的 apt/pip/npm 全部改 BuildKit `RUN --mount=type=cache`（apt lists 不再进镜像层；去掉与缓存挂载冲突的 `rm -rf /var/lib/apt/lists`） | `Dockerfile`、`web/Dockerfile`、`packages/nc-to-shp-plugin/docker/Dockerfile` |
| O-5 | 禁用 `nc_to_shp` 1.0.0 构建（`plugin_build_d1ac8c7f…`）、删除 2 条 disabled 旧调度、`docker rmi nc_to_shp:1.0.0-linux-amd64` 回收 1 GB | 线上 API + docker（见本轮部署后记录） |
| O-6 | `HUB_DEPLOY_WEBHOOK_URL` 设置后，部署成功/失败各 POST 一条 `{"text": …}`（5 s 超时、失败不连锁）；失败经 ERR 路径必达 | `deploy/ci/deploy-pipeline.sh` |
| O-10 | 业务级走查 `deploy/ci/walkthrough.sh`：登录 → 注册表核对插件 → 上传真实 NC 样本 → 建作业 → 等 SUCCESS → 清理样本；凭据读 `HUB_ADMIN_PASSWORD` 或服务器 `/opt/service-hub/.deploy-credentials`（0600，不入库），缺失则跳过不算失败 | `deploy/ci/walkthrough.sh`（新） |
| O-12 | 部署前磁盘水位：`HUB_MIN_FREE_MB`（默认 2048 MB）以下拒绝部署并在通知中告警 | `deploy/ci/deploy-pipeline.sh` |

质量门（本机，Windows）：`ruff check .` 全绿；`mypy`（87 文件）全绿；
`pytest tests/publisher tests/deploy tests/plugins tests/test_package_imports.py`
= 162 passed / 5 skipped。

> 备注：O-1 的回滚目标 tag 名为 `rollback-target`（避免与 compose/插件镜像命名冲突）；
> 服务器上原 `/opt/service-hub/build.sh` 保留作为首次部署（无上一版镜像）时的兜底。

### 执行结果与一次事故（务必知晓）

**事故：`docker image prune -f` 打断了插件作业链路（已修复，教训已固化）**

`b84f445` 部署成功后，所有新作业秒败（`Runner failed`，5 ms）。根因：hub 注册表按
**镜像 ID（digest）**引用插件运行时（`docker run sha256:<fingerprint>`），而 `nc_to_shp`
1.0.0/1.0.1 的 tag 在昨天的重建中已指向新 ID，库里记录的旧镜像成为**悬空镜像**——
本轮流水线新加的 `docker image prune -f` 把它们删了，运行器随即找不到镜像。

修复过程：
1. 从 hub 自带的安装期归档 `data/plugins/<build_id>/image.tar.zst` `docker load`
   恢复 1.0.1 的被记录镜像 `7425b3c4…`（这也是 hub 保留该归档的意义所在）；
2. 移除流水线里的 prune（`c2ce6f3` 前的 `7fd271c`），并在脚本里写明
   **本机永远不要跑 `docker image prune`**；
3. 手工补一个验证作业 → SUCCESS。

**其余执行结果（均为实测）：**

| 项 | 结果 |
|---|---|
| 自动部署 3 连发 | `b84f445` 246 s（含 Dockerfile 变更的构建 168 s）→ `7fd271c` 81 s → `c2ce6f3` 87 s（温缓存构建仅 3 s），全程无人干预 |
| 走查（O-10 修好后） | `walkthrough: job … SUCCESS — business acceptance passed`，每次部署自动做业务级验收 |
| 镜像 tag（O-1） | `1.0.0-c2ce6f3`（commit tag）+ `1.0.0-linux-amd64`（移动 tag）+ `rollback-target` 三者并存，旧 commit tag 自动清理 |
| O-5 | 调度 1/2 已删除（仅剩 id=3 每小时 1.0.1 调度）；1.0.0 构建 ENABLED→READY（禁用，无法再建作业）；`nc_to_shp:1.0.0-linux-amd64` 镜像与 staging 归档已回收 ~1.2 GB |
| 服务器侧配套 | `/opt/service-hub/.deploy-credentials`（0600，不入库）、`/opt/service-hub/samples/nc-sample.nc`（真实 HDF5 样本）已就位；`install-poller.sh` 已刷新稳定副本 |
| 健康状态 | api / web / edge 全 200，三容器 healthy，磁盘 15 G 可用 |

**新增约束（不要回退）：**

7. 部署机上**永远不要执行 `docker image prune`**——hub 按 digest 引用插件镜像，
   悬空不等于无用；要回收空间只能走插件版本管理（禁用构建 + 删除对应 tag/归档）。
8. 流水线镜像 tag 约定改动后，`compose.yaml` 仍只认 `1.0.0-linux-amd64` 移动 tag，
   不要在 compose 里直接引用 commit tag。
