# 插件系统成熟度——审查方案与执行授权（2026-09-25 定稿）

> 工作协议（用户指定）：三个智能体协作——规划 A（产品/架构）+ 规划 B（质量/运维/风险）负责大方向
> 讨论与审查，编码智能体 C 按审查通过的批次实现；讨论期间不问询用户；**本审查文档入库后才可执行**；
> 全程可回滚；完成后交付使用手册、运维文档、插件开发者指南。
>
> 流程记录：A 产出提案 v1 → B 独立审查（先建立 12 条代码事实底账，后收到 A 全文完成 28 处抽查 +
> 逐项裁定，结论"修改后通过"）→ 本文档为**调和定稿 v2**，吸收 B 全部修订 R1–R12。
> 审查记录原文见本文档附录（B v2 裁定表完整保留）。

## 0. 回滚协议（适用于全部批次）

- 锚点 tag：`plugin-maturity-base`（= 2026-09-25 全部优化完成、CI 修复前最后一个已知良好线上状态）。
- 每批一个独立 commit，`git revert <batch-commit>` + push 即回滚（自动部署 3 分钟内生效）。
- 行为变更批次（B5/B7/B4b）执行时避开每小时调度窗口，并用 walkthrough 业务走查做线上验证。
- 事故红线（2026-09-25 固化）：**任何脚本不得出现任何形式的 `docker image prune`（含 `-a`）**；
  空间回收只能走 B4 的引用感知路径（R9 静态断言测试把红线升级为门禁）。

## 1. 定稿批次（按 B v2 §3 调整后的顺序执行）

| 批次 | 内容 | 关键修订（源自 B 裁定） | 验收标准 |
|---|---|---|---|
| **B1** | G1 `hub-plugin validate` 子命令 + G12 plugin-builds 分页 | 验收含畸形包、越界路径、非白名单布局三类拒绝用例 | 新增测试全绿；CI 绿；线上回归不受影响 |
| **B6（提前）** | G10 插件开发者指南 + 历史文档标注过时 + OpenAPI 快照 + **R1 修正 CI-CD 文档 prune 残留句** + G9 密钥保管说明 | 验收方式修正（R12）：不引用不存在的 test_documented_commands，改为干净 venv 逐条冒烟指南命令（自建该测试后可引用） | 指南命令逐条可执行；走查通过 |
| **B2** | G2 `hub-plugin dev` 本地试跑（docker + 本机 python 两模式） | conda 模式在无 conda 主机上的报错必须可读 | 模板插件零 Hub 环境跑通，result.json 过协议校验；nc 插件冒烟 |
| **B3** | G3 conformance harness 参数化 + 双运行时比对工具平台化 | 第二用例用 docker 模板插件（控 CI 时长）；nc 用例先行回归 | harness 以 nc 回归通过（sha256 一致断言保留在 CI） |
| **B4a** | G4(①③)：DEPRECATED 状态 + environments 孤儿扫描（豁免 `*.tmp` 与活跃 conda 作业，R4）+ **R9 deploy 脚本禁 prune 静态断言测试** | 零迁移（status 为 String 列）；审计记录 environments 备份盲区状态 | 全链 API 测试；断言测试挂 tests/deploy |
| **B5** | G6 required 输出强制（**仅 file/files 型按文件存在性校验；object 型显式豁免并写明理由**，R5）+ datetime ISO8601 校验 + G5 per-build 并发闸（**覆盖调度触发路径**，R7） | 语义：缺失 required → FAILED `PLUGIN_OUTPUT_MISSING`；超限 → 409 `PLUGIN_CONCURRENCY_LIMIT` | 漏交必填输出 FAILED；调度被 409 后下轮自动恢复用例；nc_to_shp 线上回归 SUCCESS |
| **B4b** | G4(②)：受守卫的 DELETE。**前置条件：`HUB_DEPLOY_WEBHOOK_URL` 已配置（R10 运营项）** | 守卫链补齐（R2）：enabled Schedule 引用检查、Pipeline 步骤引用检查、active Job 含 PENDING；写明 Job 30 天 retention 对守卫的削弱与补偿；**删除顺序（R3）：先删 DB 行+storage（消除引用）→ 后 `docker image rm sha256:<id>` 精确删除，失败进对账重试**；首次执行走带 registry 留档的演练 | 全链测试 + 一次演练通过；守卫拒绝有引用构建 |
| **B7** | G7 资源限制：ExecutionSpec 可选 memory_mb/cpus（manifest 可覆盖）+ **HubSettings 平台级默认 cap（R6，默认值以 nc_to_shp 真实样本内存峰值实测取值）** + docker mem_limit/nano_cpus + conda setrlimit（语义差异写入文档） | OOMKill 退出码 → FAILED（非 TIMED_OUT）语义用测试钉住；不声明时行为不变 | 超限被杀状态正确；线上验证作业记录内存峰值 |
| **B8** | G8 结构化 failure_class + Prometheus histogram/counter + /jobs/stats 按插件聚合 | metrics.py 需扩展文本输出支持 histogram TYPE | 新指标可抓取；制造失败可归类 |
| **B9** | G9 minisign 签名 + 可选验签开关（未配置=现行为） | 密钥生成/保管说明并入开发者指南 | 篡改包被拒；未配置行为一致 |
| **B10（触发制）** | G11 插件级 RBAC/命名空间 | 出现第二业务方时立项，先做只读 ACL 最小版 | 届时另行评审 |

**新增增强批（B 排议采纳，排在 B4a 之后任意时点）**：**G4c 镜像在位性对账**（R8）——scheduler
周期校验 ENABLED 构建按 digest 的镜像存在性（现 reconcile.py 只对账长服务容器），缺失即告警
（依赖通知配置）+ 可选从 `data/plugins/<build_key>/image.tar.zst` 自动重装（恢复路径已被
2026-09-25 事故验证）。没有这一层，任何 GC 都是盲删；这也是事故模式（创建成功 → runner 5ms
秒败）的根治手段。

## 2. 批次与 O 清单映射（R11）

- O-5（1.0.0 清理）**已人工完成，本计划不得重复执行**。
- O-6 通知 bot URL、O-13 蓝绿、O-14 无关容器归属：运营前置项，不阻塞 B1–B5/B7–B9。
- O-7（Actions 核验）：已由本轮 CI 门禁 + 公开 API 轮询机制性解决。

## 3. 明确不做（A 提出、B 全部认同）

1. Hub 在线解析/编译/安装插件依赖；2. 公网插件市场/远程 registry 拉取；3. conda 容器化或 docker
容器开网络；4. FeaturePlugin 远程热插拔；5. manifest v1.0 破坏性变更（新能力一律可选字段）；
6. SQLite→PostgreSQL / 完整蓝绿 / 镜像仓库；7. **任何形式的 image prune**（含 -a，红线升级为
门禁）；8. 作业日志外接 ELK/Loki。

## 执行日志（编排者维护）

- **B1** `39e7ebd`（2026-09-26）：CI 绿灯部署 313s + 走查 SUCCESS。执行期发现：archives.py 捕获集缺 yaml.YAMLError（已另行 hotfix）。
- **B6** `491d89f`（2026-09-26）：轻量 0s 记录。开发者指南 545 行、OpenAPI 56 paths、历史文档标注。
- **B2** `90127ae`（2026-09-26）：部署 263s + 走查 SUCCESS。
- **B3** `47c2799` + 横幅补全 `7a7866b`（2026-09-26）：部署 284s + 走查 SUCCESS。
- **B4a** `1f467fd`（2026-09-26）：部署 339s + 走查 SUCCESS。发现并登记：同版本重装受唯一约束 409（无 upsert），DEPRECATED 为单向。
- **B5** `3244366` + archives hotfix `3b78fc4`/`8c4f9da`（2026-09-26）：四门全绿，部署 348s + 走查 SUCCESS（nc_to_shp 兼容性实测通过）。
- **B7** `5c89d58`（2026-09-26）：四门全绿，部署 358s + 走查 SUCCESS。**实测校准**：真实样本作业峰值内存 71.9MiB，默认 cap 2048MB 保持（28 倍余量，防失控而非贴峰值）。
- 事故记录：`3774e05` 盲改 ci.yml 截断 web/image 两 job（4 门变 2 门仍绿），`0316b51` 从历史恢复并用 tests/deploy/test_ci_workflow.py 静态断言防复发。

## 4. 执行授权

本文档随提交入库即视为审查通过、执行授权生效。编码智能体 C 按上表顺序执行，每批完成后：
本地质量门（ruff/mypy/pytest 相关片）→ 提交推送 → 等待 Actions 绿灯自动部署 → 业务走查通过 →
进入下一批。任何一批线上验证失败即 revert 并回到规划会商。

## 5. 执行日志（追加于 2026-09-26）

### B1 —— 已上线

- **commit `39e7ebd`**（2026-09-26）：G1 `hub-plugin validate` 子命令（项目/运行时/包
  三级校验，含畸形包、越界路径、非白名单布局三类拒绝用例）+ G12
  `GET /api/v1/plugin-builds` 分页（`plugin_id`/`runtime_type` 过滤，`limit` 1–200
  默认 100、`offset`，响应含全量 `total`）。
- 上线验证：CI 绿灯自动部署（313s）+ `deploy/ci/walkthrough.sh` 业务走查 **SUCCESS**
  （真实 NC 样本 docker 作业跑通）。

### B1 执行期发现（编码智能体 C 上报，待办）

- `hub_server/services/archives.py` 的 `verify_and_install` 异常捕获集缺
  `yaml.YAMLError`：上传畸形 `plugin.yaml`（YAML 语法错误而非契约校验失败）会以 500
  而非 422 返回。发布侧 `hub-plugin validate` 可在源头拦住，但服务端状态码语义应修正。
  **登记为待办，建议并入 B5 批次或单独 hotfix**（一行 except 扩展 + 一个回归测试）。
  B6 验收时已在本地临时实例**现场复现**（500 `UNEXPECTED_ERROR`，对照
  `hub-plugin validate --package` 的可读拒绝）。

### B6 —— 本批（2026-09-26）

- **G10 插件开发者指南**：新增 `docs/guides/插件开发者指南.md`，面向第二个插件作者
  覆盖从脚手架到上架的全流程：快速开始（模板 → validate → build → 安装运行）、
  plugin.yaml v1.0 字段表与上限（以 `hub_contracts` 源码为准）、SDK 使用
  （PluginContext/PluginResult/OutputFile/异常与错误码语义）、双运行时约束清单
  （docker 只读 rootfs/65532/无网络/digest 钉死；conda-pack 无容器隔离、
  GDAL_DATA/PROJ 注入、原生 Linux 构建限制）与"两种运行时必须都能跑"的验收要求、
  `.pypkg` 结构与内容哈希包缓存（`HUB_PLUGIN_CACHE`/`HUB_PLUGIN_CACHE_DIR`/
  `HUB_PLUGIN_ZSTD_LEVEL`）、API 生命周期路径（含 B1 分页参数）与纯 curl 示例、
  上线前自查表。
- **历史文档标注过时**：`构建部署使用与NC插件注册.md`、`双运行时插件构建与部署.md`
  顶部加醒目引用块指向本指南与 CI-CD 文档；删除/改写与现状明确矛盾的句子
  （"hub-server 尚未在当前分支实现"、"V1 不做认证"等历史前提均已就地标注过时）。
- **G12 相关文档**：OpenAPI 快照 `docs/openapi.json` 由 FastAPI 应用工厂导出
  （56 个 path），生成方式记录在指南 §9。
- **G9 密钥保管说明（占位）**：指南 §8 记录 minisign 方案与
  `settings.plugins.signature.public_key_path` 约定，标注"规划中（B9）"。
- 验收方式按 **R12 修正**执行：未引用不存在的 test_documented_commands，而是在干净
  环境逐条冒烟指南命令（实录见指南 §10）：validate（含畸形包拒绝）、
  `package_build.py --prepared-runtime` 真实产出 `.pypkg` 并通过 `validate --package`、
  conda-pack 构建 Windows 拒绝报错可读、docker 构建执行至 wheel+docker CLI 环节
  （本机 daemon 未就绪，完整链路由服务器 CI 覆盖）、OpenAPI JSON `json.load` 复验。
  未修改任何 `.py`/`.sh` 代码文件。

### B4a —— 本批（2026-09-25）

- **G4① DEPRECATED 状态**：新增 `POST /plugin-builds/{build_key}/deprecate`
  （publisher 守卫，写审计 `plugin_build.deprecate`）。允许 READY/ENABLED 转入，
  其余状态（INSTALLING/FAILED/DEPRECATED）409 `PLUGIN_BUILD_NOT_DEPRECATABLE`；
  DEPRECATED 单向，`enable` 拒绝回退（409 `PLUGIN_BUILD_NOT_READY`，既有守卫天然生效）。
  语义落地确认：`_resolve_enabled_build` 只认 ENABLED，DEPRECATED 后建 Job 被
  404 `PLUGIN_BUILD_NOT_ENABLED` 拒，零改动。状态为 String 列，零迁移。
- **G4③ environments 孤儿扫描（只报告）**：新增
  `services/environments.py::scan_environment_orphans`，扫描 `<data_root>/environments/`
  一级目录；孤儿 = 目录名既非任何 `PluginBuild.build_key`、也非已知 Build 的
  `{build_key}-<uuid>.tmp` 暂存；豁免活跃 conda 作业（Job status ∈ PENDING/PREPARING/
  RUNNING）引用的 Build、以及创建时间 < 1 小时的未知前缀目录（豁免并计数）。
  引用源严格以 PluginBuild 表为准（R4：永不认悬空=无用）。结果只写审计
  `environments_orphans_detected`（detail 含目录列表、数量与豁免计数）+ WARNING 日志，
  零删除能力（删除属 B4b）。
- **调度挂载选择 cleaner 模式**：扫描挂入 `deploy/service_hub/cleaner_service.py`
  主循环（与 retention sweep 同一 try/except 隔离模式），间隔复用
  `retention.sweep_interval_minutes`（30 分钟）。理由：cleaner 进程本就以该节奏唤醒，
  挂载零新增节流状态；scheduler 30 秒一跳需为磁盘扫描加模块级节流；
  kernel 插件 `SchedulerTaskSpec` 双轨目前只有声明、无消费方（部署调度器仍自注册闭包），
  挂在那里不会真正执行。
- **R9 红线升级为门禁**：新增 `tests/deploy/test_prune_ban.py`，遍历 `deploy/**/*.sh`
  断言不含 `docker image prune`/`docker system prune`/`prune -a`（大小写不敏感），
  消息标注 2026-09-25 事故红线；整行 `#` 注释豁免（部署脚本留有红线说明注释），
  行内命令不豁免；用内联 tmp 假脚本逐禁词验证断言可失败。
- **OpenAPI 快照重导**：`docs/openapi.json` 56 → 57 paths；开发者指南 §9 计数同步。
- **执行期发现（任务书冲突，如实记录）**：任务书"回退唯一途径是重新安装同版本包
  （幂等，仓库已支持）"与仓库事实不符——`uq_plugin_builds_version_platform_runtime`
  唯一约束使同版本重装必然 409 `PLUGIN_BUILD_ALREADY_EXISTS`，且 B4b 之前无删除能力，
  同版本槽位无法腾出。实际语义按"保持简单"裁定执行：DEPRECATED 单向无复活端点，
  恢复该插件版本的途径是以新版本号安装并启用新 Build；端点 docstring 已如实写明。


### B7 —— 本批（2026-09-26）

- **G7① 契约（向后兼容）**：`ExecutionSpec` 新增可选 `memory_mb: int|None`、
  `cpus: float|None`（均 `gt=0`）。v1.0 manifest 不受影响（既有 fixture 零改动，
  缺省即 None）；`0`/负数在 validate 阶段拒绝（含未知资源字段拒绝的契约用例）。
- **G7② 平台级默认 cap（R6）**：`HubSettings.runner.resource_limits` 新节点
  （`memory_mb` 默认 2048、`cpus` 默认 1.5），节点整体置 `null` 即关闭平台默认。
  生效优先级 **manifest 显式声明 > 平台默认 > 不限制**，在认领（claim）时逐次解析
  （`_resolve_resource_limits`），改默认值无需迁移存量 Build。默认值按部署机
  2 核 3.4G、同机有构建/走查/每小时调度的现状保守预置，**尚未经真实样本实测**，
  待编排者在服务器实测 nc_to_shp 内存峰值后修订（docstring 与开发者指南均已写明）。
- **G7③ docker 执行器**：`containers.run` 传 `mem_limit="{memory_mb}m"`、
  `memswap_limit` 同值（禁止 swap 超卖）、`nano_cpus=int(cpus*1e9)`；claim 无 cap
  时不传任何资源参数（行为与 B7 前一致）。传递链对照 image_digest 逐跳落地：
  `plugin_versions.manifest_json(execution)` → 认领 payload `RunnerBuildClaim`
  （+`memory_mb`/`cpus`）→ runner 服务 `_job_from_payload` → `RunnerJob` →
  `containers.run`。
- **G7④ conda 执行器（明示语义差异）**：`preexec_fn` 注入 `resource.setrlimit`，
  `RLIMIT_AS` = `memory_mb`，`RLIMIT_CPU` = `cpus` × `timeout` 秒（近似语义）。
  代码与指南均已注明：`RLIMIT_AS` 按虚拟地址空间计，GDAL/numpy 原生内存映射使其
  比 docker `mem_limit`（cgroup 常驻口径）更保守；`RLIMIT_CPU` 是累计 CPU 秒预算，
  非并发核数份额。POSIX 专属：Windows 开发机与"无 cap"路径均返回 None，行为不变；
  `resource` 模块在父进程 fork 前解析，规避 fork 后 import。
- **G7⑤ OOMKill 语义测试（R6）**：docker 用例钉住 StatusCode 137 → `FAILED`
  （非 `TIMED_OUT`，exit_code=137，container 已清理）；server 用例断言三类优先级
  到 claim payload 的最终值（默认 2048/1.5、manifest 覆盖 8192/0.75、置 null 全关）；
  settings 用例覆盖默认节点存在/置 null/非法值拒绝；runner 用例覆盖
  mem_limit=memswap_limit/nano_cpus 断言、conda preexec 真实子进程 rlimit 生效
  （POSIX-only，CI Linux 覆盖）与 payload 映射。
- **配置样例**：`config/hub.yaml.example` 增补带注释的 `resource_limits` 块。
  内部 runner 协议无 schema 变更（include_in_schema=False），OpenAPI 快照不变。
- **门禁**：ruff 全绿；mypy strict 全绿（91 文件）；pytest contracts 158 绿、
  server 535 绿、runner 65 绿 + 1 skip（POSIX rlimit 用例）——runner 全量在本机
  触发既有 0xC000013A 崩溃（`test_job_process_observes_cancellation_while_running`、
  `test_job_process_stops_when_live_event_forwarding_fails`，未改代码的 main 上同样
  复现，属本机既有问题），按预案以 `-k` 排除 3 个真实子进程终止用例后连跑 3 次全绿，
  该 3 例由 CI Linux 覆盖。
