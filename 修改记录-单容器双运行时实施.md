# 单容器双运行时实施修改记录

> 分支：`feature/single-container-hub`（工作树 `.worktrees/single-container-hub`）
> 依据计划：`docs/superpowers/plans/2026-09-12-single-container-service-hub.md`
> 本文由实施过程同步维护，按任务记录每次对仓库的修改。Task 1–7 主体由先前会话完成，此处仅记录本次会话的实际改动。

---

## Task 7 收尾：发布器摘要防篡改加固（提交 1235eb2）

接手时 Task 1–7 已提交，但 Task 7 留下 3 个先写好的失败测试（TDD RED 状态），本次实现 GREEN 并提交。

### 修改文件

| 文件 | 类型 | 内容 |
| --- | --- | --- |
| `packages/hub-publisher/src/hub_publisher/project.py` | 修改 | 新增 `PluginProject.source_digest_root()` 与 `PluginProject.source_digest()`。摘要基准统一为入口模块顶层包目录（`entrypoint.module` 首段，如 `nc_to_shp_plugin.main` → `src/nc_to_shp_plugin`），与旧 NC 专用构建脚本完全一致，保证重建已注册插件时 `source_sha256` 不分叉（Hub 端 `repositories.py` 会因同版本摘要不一致拒绝安装） |
| `packages/hub-publisher/src/hub_publisher/builder.py` | 修改 | 删除模块级 `source_digest()` 函数，`create_build_manifest` 与 Docker `--build-arg SOURCE_SHA256` 改用 `project.source_digest()`；移除未用的 `hashlib` 导入 |
| `packages/hub-publisher/src/hub_publisher/archive.py` | 修改 | `create_plugin_package` 新增 `_verify_build_matches_sources()` 校验：① `build.json` 的 `source_sha256` 必须与当前源码重算值一致（拒绝 `source_sha256` 篡改）；② conda-pack 构建的 `runtime.fingerprint` 必须与 runtime archive 实际 SHA256 一致（拒绝指纹/包体不匹配） |
| `tests/publisher/test_archive.py` | 修改 | `_sample_source_sha256()` 对齐包目录基准（`b"__init__.py\0\0"`）；新增 `test_create_plugin_package_rejects_tampered_source_digest`、`test_create_plugin_package_rejects_tampered_conda_fingerprint`（先前已写好） |
| `tests/plugins/test_package_build.py` | 修改 | 新增 `test_nc_wrapper_preserves_legacy_source_digest_basis`，锁定 NC 包装器沿用旧摘要基准（先前已写好） |

### 验证

```text
python -m pytest tests/publisher tests/plugins/test_package_build.py -q   # 17 passed
python -m ruff check packages/hub-publisher packages/nc-to-shp-plugin/scripts tests/publisher tests/plugins/test_package_build.py  # 通过
python -m mypy packages/hub-publisher/src/hub_publisher                    # 5 个文件无问题
```

提交信息：`fix: verify publisher digests against packaged sources`

---

## Task 8：两种通用插件模板（提交见 git log）

### 修改文件

| 文件 | 类型 | 内容 |
| --- | --- | --- |
| `templates/conda-script-plugin/plugin.yaml` | 新增 | 最小合法插件清单：`example_script` 1.0.0，入口 `example_plugin.main:run`，声明 `message` string 参数（默认 `hello`）与 `result` 文件输出，Python 3.12，healthcheck import |
| `templates/conda-script-plugin/environment.yml` | 新增 | conda-forge 渠道；Python 3.12.8、conda-pack 0.8.1、pydantic、PyYAML、zstandard；pip 段固定三个本地 Hub wheel（python-hub-contracts/sdk/runner==0.1.0，由构建器 wheelhouse 提供） |
| `templates/conda-script-plugin/src/example_plugin/main.py` | 新增 | 最小业务示例 `run(params, inputs, context)`：把 `message` 写入 `result.txt`，返回 `PluginResult` + `OutputFile`（保持 params/inputs/context 三参数入口契约） |
| `templates/conda-script-plugin/src/example_plugin/__init__.py` | 新增 | 包标识 |
| `templates/docker-job-plugin/plugin.yaml` | 新增 | 与 conda 模板相同的清单 |
| `templates/docker-job-plugin/Dockerfile` | 新增 | `python:3.12-slim`；安装 wheelhouse 中 Hub wheel；非 root `65532:65532`；`ENTRYPOINT ["python3", "-m", "hub_runner"]`；记录 `SOURCE_SHA256` label |
| `templates/docker-job-plugin/src/example_plugin/main.py`、`__init__.py` | 新增 | 与 conda 模板相同的业务示例 |
| `tests/publisher/test_templates.py` | 新增 | ① 两个模板都能通过 `PluginProject.load` + `validate_runtime`（conda-pack/docker 各自对应）；② 用真实 `hub_runner.execution.run_job` 端到端执行两个模板源码，断言 `result.json` 为 SUCCESS、`result.txt` 内容来自 `message` 参数 |

### 验证

```text
python -m pytest tests/publisher -q          # 17 passed（含模板 4 个）
python -m ruff check templates tests/publisher/test_templates.py  # 通过
```

提交信息：`feat: add conda and docker script plugin templates`

---

## Task 9：单容器部署文档与旧指南修订（提交见 git log）

### 修改文件

| 文件 | 类型 | 内容 |
| --- | --- | --- |
| `tests/deploy/test_documented_commands.py` | 新增 | 文档一致性门禁测试：README 不得再含 `hub-conda-runner`/`hub-docker-runner` 且必须链接新指南、含 `HUB_HOST_DATA_DIR` 与统一镜像名；新指南必须覆盖 `docker compose up -d`、health、`hubctl plugin install/file upload/job run/job download`、`hub-plugin build`、两个模板路径；迁移章节必须包含"备份""禁止"与 `docker compose down -v` 禁令；四份旧指南前 10 行必须含"旧拓扑""不再作为部署入口"标记与新指南链接 |
| `docs/guides/单容器部署与脚本插件使用.md` | 新增 | 单容器主指南，按执行顺序覆盖：安装 Docker → 导入镜像/源码构建 → 设置 `HUB_HOST_DATA_DIR` → 一条 Compose 命令启动 → health/logs/stop/restart 运维 → hubctl 注册运行插件 → hub-plugin 复制模板构建脚本插件 → Nginx 边界 → 旧数据迁移/备份/升级（含禁止 `down -v`）→ 常见错误对照表 |
| `README.md`（worktree） | 重写 | 架构图改为单容器三进程拓扑；目录表新增 hub-publisher、templates、deploy/service_hub、deploy/release、tools/hubctl；快速部署改为三条命令（build → `HUB_HOST_DATA_DIR=… docker compose up -d` → health）；新增"插件发布与运行"节演示 hub-plugin/hubctl；质量检查改为 `-m "not integration"` 与 `HUB_HOST_DATA_DIR=… docker compose config --quiet`；入口链接全部指向新指南 |
| `docs/guides/Linux-AMD64-首次部署与NC插件验收清单.md` | 修改 | 标题后插入历史标记块（旧拓扑，不再作为部署入口，指向新指南） |
| `docs/guides/双运行时插件构建与部署.md` | 修改 | 同上 |
| `docs/guides/项目总览与实施部署.md` | 修改 | 同上 |
| `docs/guides/构建部署使用与NC插件注册.md` | 修改 | 同上 |

### 验证

```text
python -m pytest tests/deploy/test_documented_commands.py -q   # 4 passed
python -m ruff check tests/deploy/test_documented_commands.py  # 通过
git diff --check                                               # 无空白错误
```

提交信息：`docs: document single-container deployment and plugin workflow`

---

## Task 10：发布验收测试与验收报告（提交见 git log）

### 修改文件

| 文件 | 类型 | 内容 |
| --- | --- | --- |
| `tests/integration/test_dual_runtime_nc_to_shp.py` | 修改 | 新增 `_compose` / `_container_health_state` / `_wait_for_container_health` / `_assert_single_container_deployment` 辅助函数；测试开始前执行部署前置断言：`docker compose config --services` 必须只含 `service-hub`，容器必须 healthy，随后执行 `restart` 并断言 health 恢复后才继续安装包与运行 Job |
| `tests/integration/test_single_container_lifecycle.py` | 新增 | 生命周期与权限边界验收（integration 标记，仅原生 Linux AMD64 执行）：部署前置断言 → 上传小文件 → `docker compose restart` → 等待 healthy → GET 文件元数据仍 200（重启持久化）→ 容器内经 supervisor Unix Socket 查询三个进程均为 RUNNING → 以 `hub-api`/`conda-runner` 身份执行 Docker ping 必须 permission denied → 以 `docker-runner` 身份 ping 必须成功 |
| `docs/reports/single-container-linux-amd64-acceptance.md` | 新增 | 验收报告模板：环境与构建物、两个插件包 SHA256、真实 NC 双运行时结果（build_id/job_id/输出 SHA256/39,464 要素/语义一致）、生命周期与权限结果、离线发布包校验、质量门记录、未通过项（必须为空）。真实 Linux 证据字段标记"待填写"，需在验收机执行对应命令后填入 |

### 验证（本机 = Windows 开发机；真实 Linux 验收项待验收机执行）

```text
python -m pytest -m "not integration" -q   # 482 passed, 3 skipped, 3 deselected
python -m ruff check .                     # 通过
python -m mypy packages                    # 53 个源文件无问题
HUB_HOST_DATA_DIR=/srv/service-hub-data docker compose config --quiet  # 通过
git diff --check                           # 通过
```

补充：为使全量非集成套件可在本机收集，向 worktree 虚拟环境安装了 `scipy==1.14.1`、
`h5py==3.12.1`、`numpy==2.1.3`（仅环境准备，不涉及仓库文件改动）。

提交信息：`test: certify single-container dual-runtime release`

---

## 质量门与遗留事项汇总

- 全量非集成质量门（pytest 482 通过 / ruff / mypy / compose config / git diff --check）全部通过。
- 真实 Linux AMD64 验收（镜像构建、两个 NC 插件包构建、真实 NC 双运行时 Job、容器生命周期、
  离线发布包安装启动）无法在本 Windows 开发机执行，需按
  `docs/reports/single-container-linux-amd64-acceptance.md` 在原生 Linux AMD64 验收机
  逐项执行并回填报告；报告"未通过项"为空后，单容器 V1 才算完成（计划检查点 C）。
- 本次会话未改动：Dockerfile、compose.yaml、deploy/service_hub、deploy/release、tools/hubctl
  （Task 1–6 主体与 Task 5–7 由先前会话完成并已提交）。
